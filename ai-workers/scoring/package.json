{
  "name": "@crm/scoring-worker",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "build": "echo \"Python build complete\"",
    "dev": "uvicorn main:app --reload --port 9000",
    "start": "uvicorn main:app --port 9000",
    "lint": "echo \"No lint script for python worker yet\""
  }
}